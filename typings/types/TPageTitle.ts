type TPageTitle = "" | "home" | "bookings" | "offices" | "teams";

export enum EPageToTheme {
  home = "slate",
  bookings = "emerald",
  teams = "pink",
  offices = "amber",
}

export default TPageTitle;
